# STATE.md — 專案戰情室狀態

> 最後更新：2026-05-29
> 對應分支：`claude/jlpt-streamlit-n1-n5-Za8UK`

---

## 🌐 當前環境
- Python 3.11+ / Streamlit / gTTS
- 啟動方式：
  ```bash
  pip install -r requirements.txt
  streamlit run app.py
  ```

## 📁 核心檔案
| 檔案 | 角色 | 狀態 |
|------|------|------|
| `app.py` | Streamlit 主程式（UI／互動／狀態） | ✅ 已完成 |
| `data.py` | 資料層（N1~N5 單字／文法／短文／50音，模擬 JSON 載入） | ✅ 已完成 |
| `requirements.txt` | 相依套件（streamlit、gTTS） | ✅ 已完成 |
| `.gitignore` | 排除 `__pycache__`、`.pyc`、venv 等 | ✅ 已完成 |
| `CLAUDE.md` | 核心開發協議 v2.0 | ✅ 已完成 |
| `STATE.md` | 本戰情室文件 | ✅ 已完成 |

## ✅ 已完成進度
- [x] 資料層與 UI 層分離（`data.py` / `app.py`）。
- [x] N1~N5 全級別單字（每級 5 筆，含六大欄位）、文法（每級 3 筆）、情境短文（每級 1 篇）。
- [x] 50音清音表（46 音），僅 N5 顯示。
- [x] Sidebar 兩層導覽 + 主頁面依級別動態切換 + 級別主題色。
- [x] 各級別獨立進度（單字學習進度 + 測驗統計），互不覆蓋。
- [x] gTTS 記憶體級（BytesIO）合成，`st.cache_data` 快取，未安裝時優雅降級。
- [x] 中翻日（選假名）進級小測驗，按級別獨立記錄正確率。

## 📌 待辦事項 (TODO)
- [ ] 擴充各級別單字量（目前每級僅 5 筆示範）。
- [ ] 50音加入濁音／半濁音／拗音表。
- [ ] 文法頁加入更多例句與分級標籤。
- [ ] 進度持久化（目前重整頁面後 session_state 會重置）。
- [ ] 加入單元測試（pytest）覆蓋 `data.py` 載入函式。

## 🐞 已知 Bug / 注意事項
- gTTS 需網路連線；離線環境下語音按鈕會自動停用（已優雅降級，非錯誤）。
- `st.session_state` 為記憶體級，**重新整理瀏覽器即清空進度**（尚未持久化）。
- 測驗的干擾選項取自同級別單字；若某級別單字 < 2 筆會停用測驗（目前皆 ≥ 5 筆，無虞）。
